Clear-Host
$time = Get-Date

<#$input = 0
do {
    $input = [int]::TryParse((ask1 = Read-Host "Enter the count", $ask2 = "Enter the factor"), [ref]$input)
    if (-not $input) {
        Write-Host "Please use a number "
    } else {
        $factor = $ask
		for ($counter =1; $counter -le $ask2; $counter++) { #counter starts at one and checks until its less then or equal to 25 
		 $answer = $counter*$factor
		 Write-Host " $factor times $counter equals $answer" # writes what number the counter is at 
    }
} while (-not $input)#>


